
# Day 28 – Leaderboard
#### Design:[https://github.com/Asabeneh/30-Days-Of-JavaScript/blob/master/images/projects/dom_mini_project_leaderboard_day_8.1.gif]

* Form nhập: **First Name, Last Name, Country, Player Score** + nút **Add Player**.
* Nếu thiếu field → hiển thị thông báo lỗi "All fields are required".
* Danh sách người chơi hiển thị:

  * Họ tên (IN HOA), ngày/giờ tạo,
  * Quốc gia, điểm,
  * 3 nút thao tác: **Delete**, **+5**, **-5**.
* Các hàng player card có nền màu block (hồng nhạt như demo).

---

## 📄 Full code 1 file (`index.html`)

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>30 Days of JavaScript Challenge Leaderboard</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #fdfdfd;
      margin: 0;
      padding: 2rem;
      color: #333;
    }
    h1 {
      text-align: center;
      margin-bottom: 1.5rem;
    }
    .form-container {
      display: flex;
      justify-content: center;
      gap: 0.5rem;
      margin-bottom: 1rem;
      flex-wrap: wrap;
    }
    .form-container input {
      padding: 0.6rem;
      border: 1px solid #ccc;
      outline: none;
      min-width: 150px;
    }
    .form-container button {
      padding: 0.6rem 1rem;
      background: #d8b5b5;
      border: none;
      cursor: pointer;
      font-weight: bold;
    }
    .error {
      text-align: center;
      color: red;
      font-weight: bold;
      margin-bottom: 1rem;
      display: none;
    }
    .player-list {
      max-width: 900px;
      margin: 0 auto;
    }
    .player-card {
      display: grid;
      grid-template-columns: 2fr 1fr 0.5fr 1fr;
      align-items: center;
      background: #e6cfcf;
      padding: 1rem;
      margin-bottom: 0.5rem;
      text-transform: uppercase;
    }
    .player-info {
      display: flex;
      flex-direction: column;
      font-size: 0.9rem;
    }
    .player-info span:first-child {
      font-weight: bold;
      font-size: 1rem;
    }
    .controls {
      display: flex;
      justify-content: flex-end;
      gap: 0.5rem;
    }
    .controls button {
      width: 35px;
      height: 35px;
      border-radius: 50%;
      border: none;
      cursor: pointer;
      font-weight: bold;
    }
    .delete {
      background: white;
      border: 1px solid red;
      color: red;
    }
    .plus {
      background: white;
      border: 1px solid green;
      color: green;
    }
    .minus {
      background: white;
      border: 1px solid orange;
      color: orange;
    }
  </style>
</head>
<body>
  <h1>30 Days Of JavaScript Challenge Leaderboard</h1>

  <div class="form-container">
    <input type="text" id="firstName" placeholder="First Name">
    <input type="text" id="lastName" placeholder="Last Name">
    <input type="text" id="country" placeholder="Country">
    <input type="number" id="score" placeholder="Player Score">
    <button id="addPlayer">Add Player</button>
  </div>
  <div class="error" id="error">All fields are required</div>

  <div class="player-list" id="playerList"></div>

  <script>
    const firstName = document.getElementById('firstName');
    const lastName = document.getElementById('lastName');
    const country = document.getElementById('country');
    const score = document.getElementById('score');
    const addPlayerBtn = document.getElementById('addPlayer');
    const error = document.getElementById('error');
    const playerList = document.getElementById('playerList');

    let players = [
      { firstName: 'Martha', lastName: 'Yohanes', country: 'Finland', score: 85 },
      { firstName: 'David', lastName: 'Smith', country: 'United Kingdom', score: 80 },
      { firstName: 'Asabeneh', lastName: 'Yetayeh', country: 'Finland', score: 75 },
      { firstName: 'Mathias', lastName: 'Elias', country: 'Sweden', score: 70 }
    ];

    function formatDate() {
      const now = new Date();
      const options = { year: 'numeric', month: 'short', day: '2-digit',
                        hour: '2-digit', minute: '2-digit' };
      return now.toLocaleDateString('en-US', options);
    }

    function renderPlayers() {
      playerList.innerHTML = '';
      players.sort((a, b) => b.score - a.score);
      players.forEach((p, index) => {
        const card = document.createElement('div');
        card.className = 'player-card';

        const info = document.createElement('div');
        info.className = 'player-info';
        info.innerHTML = `<span>${p.firstName} ${p.lastName}</span>
                          <span>${formatDate()}</span>`;
        const countryDiv = document.createElement('div');
        countryDiv.textContent = p.country;
        const scoreDiv = document.createElement('div');
        scoreDiv.textContent = p.score;

        const controls = document.createElement('div');
        controls.className = 'controls';
        const delBtn = document.createElement('button');
        delBtn.className = 'delete';
        delBtn.innerHTML = '🗑';
        delBtn.onclick = () => {
          players.splice(index, 1);
          renderPlayers();
        };

        const plusBtn = document.createElement('button');
        plusBtn.className = 'plus';
        plusBtn.textContent = '+5';
        plusBtn.onclick = () => {
          players[index].score += 5;
          renderPlayers();
        };

        const minusBtn = document.createElement('button');
        minusBtn.className = 'minus';
        minusBtn.textContent = '-5';
        minusBtn.onclick = () => {
          players[index].score -= 5;
          renderPlayers();
        };

        controls.appendChild(delBtn);
        controls.appendChild(plusBtn);
        controls.appendChild(minusBtn);

        card.appendChild(info);
        card.appendChild(countryDiv);
        card.appendChild(scoreDiv);
        card.appendChild(controls);

        playerList.appendChild(card);
      });
    }

    addPlayerBtn.addEventListener('click', () => {
      if (!firstName.value || !lastName.value || !country.value || !score.value) {
        error.style.display = 'block';
        return;
      }
      error.style.display = 'none';

      players.push({
        firstName: firstName.value,
        lastName: lastName.value,
        country: country.value,
        score: parseInt(score.value)
      });

      firstName.value = '';
      lastName.value = '';
      country.value = '';
      score.value = '';

      renderPlayers();
    });

    renderPlayers();
  </script>
</body>
</html>
```

---

📌 Tính năng:

* Nhập đủ **First, Last, Country, Score** → Thêm vào leaderboard.
* Nếu thiếu → báo lỗi đỏ *"All fields are required"*.
* Mỗi player có:

  * **Tên + ngày/giờ thêm**
  * Quốc gia
  * Điểm
  * Nút 🗑 **xóa**, **+5**, **-5** (tự động cập nhật & sắp xếp lại).

